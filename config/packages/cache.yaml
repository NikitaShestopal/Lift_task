framework:
    cache:
        app: cache.adapter.redis
        default_redis_provider: '%env(resolve:REDIS_URL)%'
        prefix_seed: my_symfony_mongo_app

        pools:
            doctrine_mongodb.system_cache_pool:
                adapter: cache.adapter.redis
